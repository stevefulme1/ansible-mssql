---
argument_specs:
  main:
    short_description: "Configure monitoring, alerts, and extended events"
    options:
      mssql_monitoring_host:
        description: Target host address
        type: str
        required: true
      mssql_monitoring_username:
        description: Authentication username
        type: str
      mssql_monitoring_password:
        description: Authentication password
        type: str
        no_log: true
      mssql_monitoring_api_key:
        description: API key for authentication
        type: str
        no_log: true
      mssql_monitoring_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      mssql_monitoring_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
